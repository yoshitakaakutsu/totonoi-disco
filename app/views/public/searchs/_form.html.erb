
  
  <div class="col-lg-2">
  <%= form_with url: search_path, local: true, method: :get do |f| %>
    <%= f.text_field :content %>
    <%= f.select 'model', options_for_select({ "ユーザー名" => "user", "投稿" => "post" }) %>
    <%= f.submit '検索' %>
  <% end %>
  </div>